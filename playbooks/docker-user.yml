---
- name: Configure Docker user permissions
  hosts: all
  become: true

  tasks:
    - name: Add deploy user to docker group
      ansible.builtin.user:
        name: deploy
        groups: docker
        append: true
